# UserManagementAPI
This project is a C# ASP.NET Core Web API for managing users with full CRUD functionality (Create, Read, Update, Delete). It includes input validation, custom logging middleware, and follows REST API standards. You can test all endpoints easily using Swagger UI by running the project and opening the generated Swagger page in your browser.
